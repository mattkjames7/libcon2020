#ifndef __TESTS_H__
#define __TESTS_H__
#include <iostream>
#include <filesystem>
#include <vector>
#include <tuple>
#include <fstream>
#include "testdata.h"
#include <con2020.h>

typedef std::tuple<std::vector<double>,std::vector<double>,std::vector<double>> tupleVectors;
typedef std::tuple<tupleVectors,tupleVectors> posFieldVectors;

#endif
